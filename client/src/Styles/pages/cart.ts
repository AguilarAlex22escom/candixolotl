import { snow, shadow, tobaco } from "../defaultStyles";

export const SaleBoxCart = {
    color: snow,
    backgroundColor: tobaco,
    boxShadow: `0 0 8px ${shadow}`,
    borderRadius: "8px",
    margin: "16px",
    padding: "16px",
    textAlign: "center",
}