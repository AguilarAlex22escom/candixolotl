import { font } from "../defaultStyles";

export const StyledAdderSubstracter = {
    display: "flex",
    fontFamily: font,
    fontSize: "1em",
    width: "100%",
    justifyContent: "space-around",
    alignItems: "center",
    margin: "8px 0 8px 0",
}